# Email-Spam-Detection-Model
This project builds an Email Spam Detection Model using the Naive Bayes classification algorithm. Email messages are converted into numerical features using CountVectorizer. The model is trained on labeled email data and learns to distinguish between spam and non-spam messages.
